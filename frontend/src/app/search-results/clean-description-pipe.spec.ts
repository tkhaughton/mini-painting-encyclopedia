import { CleanDescriptionPipe } from './clean-description-pipe';

describe('CleanDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new CleanDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
