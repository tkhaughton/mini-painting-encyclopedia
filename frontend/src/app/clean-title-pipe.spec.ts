import { CleanTitlePipe } from './clean-title-pipe';

describe('CleanTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new CleanTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
